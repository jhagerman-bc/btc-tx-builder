export enum Justify {
  center = "center",
  start = "start",
  end = "end",
  flexStart = "flex-start",
  flexEnd = "flex-end",
  left = "left",
  right = "right",
}

export enum AlignItem {
  stretch = "stretch",
  center = "center",
  start = "start",
  end = "end",
  flexStart = "flex-start",
  flexEnd = "flex-end",
  baseline = "baseline",
}

export enum FlexDirection {
  row = "row",
  column = "column",
}
